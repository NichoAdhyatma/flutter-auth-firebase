class Product {
  String id, title, price;
  DateTime createdAt, updatedAt;

  Product({
    required this.id,
    required this.title,
    required this.price,
    required this.createdAt,
    required this.updatedAt,
  });
}
